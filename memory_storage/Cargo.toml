[package]
name = "openmls_memory_storage"
authors = ["OpenMLS Authors"]
version = "0.5.0"
edition = "2021"
rust-version.workspace = true
description = "A very basic storage for OpenMLS implementing openmls_traits."
license = "MIT"
documentation = "https://docs.rs/openmls_memory_storage"
repository = "https://github.com/openmls/openmls/tree/main/memory_storage"
readme = "README.md"

[dependencies]
openmls_traits = { workspace = true }
maybe-async = { workspace = true }

thiserror = { workspace = true }
serde_json = "1.0"
serde = { workspace = true, features = ["derive"] }
log = { version = "0.4" }
hex = { version = "0.4", features = ["serde"], optional = true }
base64 = { version = "0.22", optional = true }

[features]
# Mirrors the other member crates: `sync` collapses the maybe_async impl back
# to the blocking shape. Hardcoding `openmls_traits/sync` here used to flip
# `maybe-async/is_sync` on for every graph that pulled this crate in.
sync = ["openmls_traits/sync", "maybe-async/is_sync"]
async = ["openmls_traits/async"]
test-utils = ["hex", "openmls_traits/test-utils"] # Enable test utilites
persistence = ["base64"]
extensions-draft = ["openmls_traits/extensions-draft"]
draft-ietf-mls-pq-ciphersuites = [
    "openmls_traits/draft-ietf-mls-pq-ciphersuites",
]
virtual-clients-draft = [
    "extensions-draft",
    "openmls_traits/virtual-clients-draft",
]

[dev-dependencies]
openmls_memory_storage = { path = ".", features = ["test-utils"] }
