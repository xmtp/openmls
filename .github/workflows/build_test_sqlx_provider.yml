name: Build & test sqlx storage provider crate

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  tests:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            sqlx_storage -> target
      - name: Build openmls_sqlx_storage
        run: |
          cargo build --manifest-path sqlx_storage/Cargo.toml
      - name: Test openmls_sqlx_storage
        run: |
          cargo test --manifest-path sqlx_storage/Cargo.toml
      - name: Build openmls_sqlx_storage with `extensions-draft-08` feature
        run: |
          cargo build -F extensions-draft-08 --manifest-path sqlx_storage/Cargo.toml
      - name: Test openmls_sqlx_storage with `extensions-draft-08` feature
        run: |
          cargo test -F extensions-draft-08 --manifest-path sqlx_storage/Cargo.toml
      # The PostgreSQL round-trip tests need a live server and skip without
      # `OPENMLS_SQLX_POSTGRES_URL`, so CI only checks that the backend builds,
      # both on its own and alongside the SQLite one.
      - name: Build openmls_sqlx_storage with only the `postgres` backend
        run: |
          cargo build --no-default-features -F postgres --manifest-path sqlx_storage/Cargo.toml
      - name: Build openmls_sqlx_storage with all features
        run: |
          cargo build --all-features --manifest-path sqlx_storage/Cargo.toml
